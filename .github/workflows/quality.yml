name: Quality

on:
  push:
    branches: [main]
  pull_request:

jobs:
  mise-doctor:
    name: Mise
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v4
        env:
          # ctx7's postinstall otherwise waits out the full 10-minute OAuth
          # device-code lifetime here, since no one can approve it on a runner.
          # setup only stores the key, it never validates it.
          CONTEXT7_API_KEY: ci-placeholder-not-a-real-key
      - run: mise doctor
      - name: Validate setup tasks parse
        run: mise tasks ls

  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ludeeus/action-shellcheck@master

  syntax:
    name: Shell syntax
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: bash -n
        run: |
          bash -n install.sh
          bash -n .claude/statusline.sh
      - name: zsh -n
        # install.sh is documented to run under zsh too (see README), so
        # verify it parses cleanly there in addition to bash.
        run: |
          sudo apt-get update && sudo apt-get install -y zsh
          zsh -n install.sh
