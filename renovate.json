{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "minimumReleaseAge": "2 days",
  "internalChecksFilter": "strict",
  "packageRules": [
    {
      "description": "Hold every mise tool bump in a single weekly PR, so the dotfiles update notification stays meaningful instead of firing on every upstream release",
      "matchManagers": [
        "mise"
      ],
      "groupName": "mise tools",
      "schedule": [
        "before 5am on monday"
      ]
    },
    {
      "description": "Digest pins are not a new release, so the age check would only stall them",
      "matchUpdateTypes": [
        "pinDigest"
      ],
      "minimumReleaseAge": null
    }
  ]
}
